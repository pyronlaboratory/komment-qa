/**
 * @description Nothing; it logs the string "test" to the console but doesn't return
 * anything or take any arguments. It is a functions with no effect.
 */
const func0 = () => { 
  console.log("test");
}
