/**
 * @description Nothing; it's an empty function. When the parentheses are empty and
 * there is no statement inside them (i.e., no content within the braces), an arrow
 * function expression (written here as `() =>`) evaluates to `undefined`. Here's the
 * function being defined and called without the return value: `() => { console.log("test");
 * }`.
 */
const func0 = () => { 
  console.log("test"); 
  
}
