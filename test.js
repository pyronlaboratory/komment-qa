
/**
 * @description This is an immediately invoked function expression (IIFE) that simply
 * logs the string "0" to the console and then exits the current script using the
 * `exit` keyword.
 */
const func0 = () => { 
  console.log("0"); 
  exit;
}
