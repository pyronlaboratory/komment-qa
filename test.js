/**
 * @description This function does nothing; it is a function with no body and will
 * always return undefined since it has no returning statement.
 */
const funcn0 = () => { 
  console.log("test");
}

