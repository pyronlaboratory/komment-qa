
/**
 * @description This function does nothing because it is an empty function: it has
 * no statements after the curly braces.  Therefore it will not print "test" to the
 * console when called.
 */
const funcn0 = () => { 
  
  console.log("test"); 
}
